
<% if @current_user.userType == 'admin'%>
<td><%= link_to 'CHARGE ACCOUNTS', charge_account_users_path ,class: 'btn btn-dark'%></td>

<h1> BUYERS</h1>
<br/>
<br/>
<table class='table table-striped'>
  <thead>
    <tr>
      <th>USER</th>
      <th>STATUS</th>
      <th colspan='2'></th>
    </tr>
  </thead>
  <tbody>
    <% @user.each do |user| %>
        <tr>
        <% if user.userType != 'admin'%>
          <td><%= user.email %></td>
          <td><%= link_to 'SUBSCRIBED PLANS', user_plan_usages_path(user.id) ,class: 'btn btn-dark'%></td>
          <td><%= link_to 'DELETE USER', user_path(user.id),class: 'btn btn-dark', method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <%end%>
    <% end %>
  </tbody>
</table>
  <%else%>
  <%end%>
<br>


